{
  "name": "scenes",
  "version": "0.0.1",
  "scripts": {
    "start": "browser-sync start --files='index.html, css/**/*.css, js/**/*.js' --server"
  },
  "devDependencies": {
    "browser-sync": "^2.18.13"
  },
  "private": true
}
